
 <!DOCTYPE html>
 <html lang="en">
 <head>
     <title>E-Park</title>
 </head>
    <body>

            <% for(var i = 0; i < slots.length;i++) { %>
                No.<input value="<%= slots[i].slot_id; %>"  name="Number">,
                Qr code <input value="<%= slots[i].qr_code; %>" name="Qr_code"> <br>
            <% } %>
       
    </body>
 </html>
